# flimfitutils

Utility programs for analysis of (metabolic) FLIM data in Python.

Dependencies:

Dependencies are in `pyproject.toml`.
Or you can uv add the following packages:

- numpy
- pillow
- scipy
- matplotlib